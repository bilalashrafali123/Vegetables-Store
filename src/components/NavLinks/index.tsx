import './index.scss';

const NavLinks = () => {
  return (
    <div className="navlinks">
      <a href="#" className="active">Home</a>
      <a href="#">Shop</a>
      <a href="#">Shop Detail</a>
      <a href="#">Pages ▾</a>
      <a href="#">Contact</a>
    </div>
  );
};

export default NavLinks;
